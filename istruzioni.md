python -m venv .venv 

source .venv/bin/activate (linux/macos)

.venv/Script/activate.bat (windows)

deactivate (linux/macos/windows) si disattiva il virtual enviroment

pip freeze

pip freeze > requirements.txt  (linux/macos)